---
const { fileImg, alt } = Astro.props;
const fixedImgPath = fileImg;
---

<div>
    <img src={fixedImgPath} alt={alt} />
</div>

<style>
    div {
        position: relative;
        width: 163px;
        aspect-ratio: 1;
        border: 10px solid var(--clr-accent-planet);
        border-radius: 50%;
        padding: 0;
    }

    /* div::before {
        --tooltip-height: 40px;
        position: absolute;
        content: "";
        width: 32px;
        height: 40px;
        top: calc(2px - 40px);
        left: calc(163px / 2 - 40px / 2);
        clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
        background-color: var(--clr-accent-planet);
    } */

    img {
        height: 129px;
        aspect-ratio: 1;
        border-color: #ffffff;
        border-width: 6px;
        border-radius: 50%;
        overflow: hidden;
    }
</style>
